# Direct dependencies only. Transitive dependencies (httpx, pydantic,
# numpy, onnxruntime, etc.) are installed automatically by pip.
#
# Do NOT regenerate this with `pip freeze` — that captures everything in
# the virtualenv, including packages from unrelated experiments.

anthropic==0.104.1
chromadb==1.5.9
Flask==3.1.3
gunicorn==26.0.0
phonenumbers==9.0.33
python-dotenv==1.2.2
PyYAML==6.0.3
twilio==9.10.9
voyageai==0.5.0